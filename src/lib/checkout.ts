// TODO: replace with your real Lemon Squeezy checkout URLs once the products are live.
// Dashboard → Products → (variant) → Copy checkout URL.
// Shared between the Landing page pricing cards and the in-app PaywallModal
// so both always point at the same two plans.
export const MONTHLY_CHECKOUT_URL = "https://YOUR-STORE.lemonsqueezy.com/checkout/buy/MONTHLY-VARIANT-ID";
export const ANNUAL_CHECKOUT_URL = "https://YOUR-STORE.lemonsqueezy.com/checkout/buy/ANNUAL-VARIANT-ID";
